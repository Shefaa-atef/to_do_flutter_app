# Flutter Internals: Todo Demo

This folder contains the Flutter application. See the [project README](../README.md) for features, SDK requirements, setup, and current limitations.

From this directory, after completing the prerequisites:

```sh
flutter pub get
flutter run
```

Run `flutter analyze` and `flutter test` here for development checks.
